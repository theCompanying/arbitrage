{
  "name": "arbitrage-business",
  "version": "0.1.0",
  "private": true,
  "description": "AliExpress to Amazon arbitrage business",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "start": "pnpm --filter web start",
    "lint": "pnpm --filter web lint",
    "test": "echo \"No tests specified yet\" && exit 0"
  },
  "keywords": [
    "arbitrage",
    "ecommerce",
    "amazon",
    "aliexpress"
  ],
  "author": "",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20.0.0"
  }
}
